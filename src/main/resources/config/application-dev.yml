logging:
  level:
    root: DEBUG
    org.hibernate.SQL: DEBUG
    net.hypnoz.hpzgs: DEBUG

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false
  jackson:
    serialization:
      indent-output: true
  liquibase:
    contexts: dev, faker
    url: jdbc:postgresql://localhost:5432/hypnoz
    driver-class-name: org.postgresql.Driver
  mail:
    host: localhost
    port: 25
    username:
    password:
  messages:
    cache-duration: PT1S
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/hypnoz
    username: hypnoz
    password: Supercreator5.
  thymeleaf:
    cache: false
server:
  port: 9894

hypnoz:
  cors:
    # Allow Ionic for JHipster by default (* no longer allowed in Spring Boot 2.4+)
    # Ports 4201-4209 are allowed for microfrontend development
    allowed-origins: 'http://localhost:9894,https://localhost:8100,http://localhost:9000,https://localhost:9000,http://localhost:4200,https://localhost:4200,http://localhost:4201,https://localhost:4201,http://localhost:4202,https://localhost:4202,http://localhost:4203,https://localhost:4203,http://localhost:4204,https://localhost:4204,http://localhost:4205,https://localhost:4205,http://localhost:4206,https://localhost:4206,http://localhost:4207,https://localhost:4207,http://localhost:4208,https://localhost:4208,http://localhost:4209,https://localhost:4209'
    # Enable CORS when running in GitHub Codespaces
    allowed-origin-patterns: 'https://*.githubpreview.dev'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count,X-${hypnoz.clientApp.name}-alert,X-${hypnoz.clientApp.name}-error,X-${hypnoz.clientApp.name}-params'
    allow-credentials: true
    max-age: 1800

  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        base64-secret: MmZiZWM3MjQwNmVmOGZlNTc0MjdjMTVmODFmM2RlMWVkZjIzZjY5ODVkMzBlMmQ0ZGY1M2Y1NGZjM2Y4Mjk1MDg3Yjk5NjUzZTMzZGZkODVmMjA0OTYzODJlMWJjZTRjNjVhMWIyNjliZGFkYjI3NGYwOWJmN2JjMjQzM2VjODg=
        # Token is valid 24 hours
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  mail: # specific JHipster mail property, for standard properties see MailProperties
    base-url: http://127.0.0.1:9894
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      queue-size: 512
